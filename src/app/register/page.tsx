import RegisterPage from '@/components/users/Register';
import React from 'react';

const page = () => {
    return (
        <div>
            <RegisterPage/>
        </div>
    );
};

export default page;